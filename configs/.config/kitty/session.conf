# Set the window layout for the current tab
layout fat
# Set the working directory for windows in the current tab
cd ~/projects/grantmacken/dorex
# launch nvim -S "$HOME/.cache/nvim/session/dorex"
# title dorex

# new_tab dotfiles
# cd ~/projects/grantmacken/dotfiles
# enbled_layouts tall, stack
# launch nvr -s "README.md"
# Set the current layout

